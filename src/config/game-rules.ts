/** 3. çağ = indeks 2 (yeniden_dogus) ve sonrası — yeni şehir kurma */
export const MIN_ERA_INDEX_FOR_FOUND_CITY = 2;

/** İmparatorluk araştırması minimum seviye (şehir kurma) */
export const MIN_RESEARCH_TIER_FOR_FOUND_CITY = 3;

/** Pazar ilanı: bu süre (sn) dolmadan alım yapılamaz */
export const MARKET_OPEN_DELAY_SEC = 180;
